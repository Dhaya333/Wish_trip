# PostgreSQL connection string. For quick local testing you may instead
# leave this unset and DATABASE_URL will fall back to a local SQLite file
# (see database/session.py) -- not recommended for anything beyond dev.
DATABASE_URL=postgresql+psycopg2://wishtrip:wishtrip@localhost:5432/wishtrip_goa

# Backend URL used by the Streamlit frontend
BACKEND_URL=http://localhost:8000